spring:
  datasource:
    url: jdbc:h2:mem:library;MODE=postgresql
    # schema: schema.sql
    # data: data.sql

  h2:
    console:
#      path: /h2-console
#      settings:
#        web-allow-others: true
  liquibase:
    enabled: true